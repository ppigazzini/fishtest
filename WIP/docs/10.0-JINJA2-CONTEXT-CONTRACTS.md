# Jinja2 template context contracts (idiomatic target)

Date: 2026-02-07
Status: Draft (pre-code baseline)

This document defines the **explicit context contract** for each Jinja2 template in
`server/fishtest/templates_jinja2/`. The intent is to remove hidden dependencies
on request internals and move all data shaping into the view layer or helpers.

## Conventions

- Templates should not fetch data. Views provide fully shaped context.
- The request object is not used directly in templates, except via `url_for` and
  `static_url` helpers passed in context.
- Prefer preformatted display strings (use `*_label`) and explicit URLs (`*_url`).
- Prefer structured objects over ad-hoc dict access in templates.
- JS data must be passed as JSON (`|tojson`) and not interpolated into JS strings.

## Shared base context (all pages)

Required keys (used by base layout):
- `static_url(path: str) -> str` for static asset URLs.
- `url_for(name: str, **params) -> str` for named routes.
- `csrf_token` (string) for meta tag and form hidden inputs.
- `theme` (string, `"dark"` or `"light"`, or empty) for theme toggles.
- `current_user` (object or null):
  - `username` (string) and `id` (string or int) if authenticated.
- `pending_users_count` (int) for the user-management badge.
- `flash` (dict): `error`, `warning`, `info` lists (each list contains strings).

Optional keys:
- `page_title` (string) if the base template owns the `<title>` block.

---

## Template contracts

### base.mak
Required context keys:
- Uses **Shared base context** only.

---

### actions.mak
Required context keys:
- `actions` (list of action rows), each row has:
  - `time_unix` (int)
  - `time_label` (string, already formatted)
  - `event` (string)
  - `agent_name` (string)
  - `agent_url` (string or null)
  - `target_name` (string or empty)
  - `target_url` (string or null)
  - `message` (string or empty)
- `filters` (dict):
  - `action` (string)
  - `username` (string)
  - `text` (string)
  - `run_id` (string or empty)
- `usernames` (list of strings) for the datalist.
- `pages` (list of pagination items; see pagination.mak).
- `is_approver` (bool).

Optional keys:
- `action_url` (string) if the template uses a prebuilt URL for pagination links.

---

### contributors.mak
Required context keys:
- `is_monthly` (bool).
- `is_approver` (bool).
- `summary` (dict): `testers`, `developers`, `active_testers`, `cpu_years`, `games`, `tests`.
- `users` (list), each user has:
  - `username` (string)
  - `last_updated_label` (string)
  - `last_updated_sort` (number)
  - `games_per_hour` (int)
  - `cpu_hours` (int)
  - `games` (int)
  - `tests` (int)
  - `tests_repo` (string)
  - `tests_repo_url` (string)
  - `tests_user_url` (string)

---

### elo_results.mak
Required context keys:
- `elo` (dict):
  - `info_lines` (list of strings)
  - `pre_attrs` (string of HTML attributes for the `<pre>`)
  - `show_gauge` (bool)
  - `chart_div_id` (string)
  - `nelo_summary_html` (string or null, already safe HTML)
  - `live_elo_url` (string or null)
  - `is_sprt` (bool)

---

### login.mak
Required context keys:
- None beyond Shared base context.

---

### machines.mak
Required context keys:
- `machines` (list), each row has:
  - `username` (string)
  - `country_code` (string or null)
  - `concurrency` (int)
  - `worker_url` (string)
  - `worker_short` (string)
  - `nps_m` (string, preformatted)
  - `max_memory` (string)
  - `system` (string)
  - `worker_arch` (string)
  - `compiler_label` (string)
  - `python_label` (string)
  - `version_label` (string)
  - `run_url` (string)
  - `run_label` (string)
  - `last_active_label` (string)
  - `last_active_sort` (number)

---

### nn_upload.mak
Required context keys:
- `upload_url` (string)
- `testing_guidelines_url` (string)
- `cc0_url` (string)
- `nn_stats_url` (string)

---

### nns.mak
Required context keys:
- `filters` (dict): `network_name`, `user`, `master_only`.
- `pages` (list of pagination items; see pagination.mak).
- `nns` (list), each row has:
  - `time_label` (string)
  - `name` (string)
  - `name_url` (string)
  - `user` (string)
  - `first_test_label` (string or empty)
  - `first_test_url` (string or empty)
  - `last_test_label` (string or empty)
  - `last_test_url` (string or empty)
  - `downloads` (int)
  - `is_master` (bool)

---

### notfound.mak
Required context keys:
- None beyond Shared base context.

---

### pagination.mak
Required context keys:
- `pages` (list of items), each item has:
  - `idx` (string or int)
  - `url` (string)
  - `state` (string: `active`, `disabled`, or empty)

---

### rate_limits.mak
Required context keys:
- None beyond Shared base context.

---

### run_table.mak
Required context keys:
- `header` (string or null)
- `count` (int or null)
- `page_idx` (int)
- `username` (string or empty)
- `title_suffix` (string)
- `toggle` (string or null)
- `toggle_state` (string: `"Hide"` or `"Show"`)
- `pages` (list of pagination items; see pagination.mak)
- `show_delete` (bool)
- `alt` (string or null)
- `runs` (list), each run has:
  - `id` (string)
  - `start_date_label` (string)
  - `user_short` (string)
  - `user_name` (string)
  - `user_url` (string)
  - `is_finished` (bool)
  - `is_sprt` (bool)
  - `new_tag_short` (string)
  - `run_url` (string)
  - `diff_url` (string)
  - `elo_context` (dict for elo_results.mak)
  - `tc` (string)
  - `threads` (int)
  - `num_games` (int)
  - `cores_label` (string)
  - `info` (string)

---

### run_tables.mak
Required context keys:
- `page_idx` (int)
- `prefix` (string)
- `runs` (dict):
  - `pending` (list of run rows)
  - `active` (list of run rows)
- `failed_runs` (list of run rows)
- `finished_runs` (list of run rows)
- `num_finished_runs` (int)
- `finished_runs_pages` (list of pagination items)

---

### signup.mak
Required context keys:
- `csrf_token` (string)
- `recaptcha_site_key` (string)

---

### sprt_calc.mak
Required context keys:
- None beyond Shared base context.

---

### tasks.mak
Required context keys:
- `tasks` (list), each row has:
  - `task_id` (int)
  - `row_class` (string)
  - `worker_label` (string)
  - `worker_url` (string or empty)
  - `info_label` (string)
  - `last_updated_label` (string)
  - `played_label` (string)
  - `results_cells` (list of strings)
  - `crashes` (string)
  - `time_losses` (string)
  - `residual_label` (string or empty)
  - `residual_bg` (string or empty)
- `show_pentanomial` (bool)
- `show_residual` (bool)

---

### tests.mak
Required context keys:
- `page_idx` (int)
- `cores` (int)
- `nps_m` (string)
- `games_per_minute` (string)
- `pending_hours` (string)
- `machines_shown` (bool)
- `machines_count` (int)
- `run_tables_ctx` (dict for run_tables.mak)

---

### tests_finished.mak
Required context keys:
- `filters` (dict): `ltc_only`, `success_only`, `yellow_only` (bools)
- `title_suffix` (string)
- `finished_runs` (list of run rows)
- `num_finished_runs` (int)
- `finished_runs_pages` (list of pagination items)

---

### tests_live_elo.mak
Required context keys:
- `run_id` (string)
- `page_title` (string)
- `run_url` (string)

---

### tests_run.mak
Required context keys:
- `args` (dict with current form values)
- `setup` (dict):
  - `base_branch`, `latest_bench`, `pt_version`, `pt_branch`, `pt_signature`
  - `tc`, `new_tc`, `default_book`, `is_odds`, `arch_filter`, `compiler`
- `test_book` (string)
- `pt_book` (string)
- `valid_books` (list of strings)
- `supported_compilers` (list of strings)
- `supported_arches` (list of strings)
- `tests_repo` (string)
- `is_rerun` (bool)
- `rescheduled_from` (string or empty)
- `master_info` (dict with `message`, `date`)
- `pt_info` (dict, when needed for PT context)
- `format_bounds` (helper function or preformatted bounds strings)

---

### tests_stats.mak
Required context keys:
- `page_title` (string)
- `run_id` (string)
- `has_spsa` (bool)
- `stats` (dict) with sections:
  - `context`:
    - `tc`, `new_tc`, `book`, `threads`, `base_options`, `new_options`
  - `sprt` (optional when has_sprt):
    - `alpha`, `beta`, `elo0`, `elo1`, `elo_model`, `batch_size_games`
  - `draws`:
    - `draw_ratio`, `draw_elo`, `pentanomial_draw_ratio` (optional)
  - `trinomial`:
    - `elo`, `elo_ci_low`, `elo_ci_high`, `los`, `llr` (optional)
    - `games`, `results`, `pdf`, `avg`, `var`, `skew`, `exkurt`
    - `score`, `score_ci_low`, `score_ci_high`
    - `var_game`, `var_game_ci_low`, `var_game_ci_high`
    - `stdev_game`, `stdev_game_ci_low`, `stdev_game_ci_high`
    - `nelo`, `nelo_ci_low`, `nelo_ci_high`
    - `llr_jumps` (optional)
  - `pentanomial` (optional):
    - `elo`, `elo_ci_low`, `elo_ci_high`, `los`, `llr` (optional)
    - `games`, `results`, `pdf`, `dd_wl_split`, `avg`, `var`, `skew`, `exkurt`
    - `score`, `score_ci_low`, `score_ci_high`
    - `var_game`, `var_game_ci_low`, `var_game_ci_high`
    - `stdev_game`, `stdev_game_ci_low`, `stdev_game_ci_high`
    - `nelo`, `nelo_ci_low`, `nelo_ci_high`
    - `llr_jumps` (optional), `overshoot` (optional)
  - `comparison` (optional):
    - `variance_ratio`, `variance_diff`, `rms_bias`, `rms_bias_elo`

---

### tests_user.mak
Required context keys:
- `username` (string)
- `is_approver` (bool)
- `run_tables_ctx` (dict for run_tables.mak)

---

### tests_view.mak
Required context keys:
- `page_title` (string)
- `run_id` (string)
- `run` (dict with run summary fields used by the page)
- `run_args` (list of `[name, value, url]` tuples)
- `document_size` (int)
- `approver` (bool)
- `warnings` (list of HTML strings)
- `notes` (list of HTML strings)
- `can_modify_run` (bool)
- `same_user` (bool)
- `chi2` (dict with `chi2`, `dof`, `p`)
- `tasks_shown` (bool)
- `tasks_total` (string)
- `show_task` (int)
- `follow` (int)
- `spsa_data` (dict or null)
- `allow_github_api_calls` (bool)
- `use_3dot_diff` (bool)
- `official_master_sha` (string or null)
- `tests_repo_url` (string)
- `diff_url` (string)

---

### user.mak
Required context keys:
- `profile` (bool)
- `user` (dict): `username`, `email`, `tests_repo`, `registration_time`, `groups`, `pending`, `blocked`
- `limit` (string)
- `hours` (string)
- `format_date` (helper function or preformatted string)
- `format_group` (helper function or preformatted string)
- `extract_repo_from_link` (string)
- `post_url` (string)

---

### user_management.mak
Required context keys:
- `all_users`, `pending_users`, `blocked_users`, `idle_users`, `approvers_users` (lists)
- Each user entry has: `username`, `registration_time_label`, `groups_label`, `email`

---

### workers.mak
Required context keys:
- `show_admin` (bool)
- `worker_name` (string)
- `last_updated_label` (string or null)
- `message` (string)
- `blocked` (bool)
- `show_email` (bool)
- `blocked_workers` (list), each row has:
  - `worker_name` (string)
  - `worker_url` (string)
  - `last_updated_label` (string)
  - `actions_url` (string)
  - `owner_email` (string, optional)
  - `owner_email_url` (string, optional)
